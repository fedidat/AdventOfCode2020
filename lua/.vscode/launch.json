
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lrdb",
            "request": "launch",
            "name": "Lua Launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        },
        {
          "type": "lua-local",
          "request": "launch",
          "name": "Debug",
          "program": {
            "lua": "lua",
            "file": "${file}"
          }
        }
    ]
}